# index.html is generated by build.js from src/app.html + data/.
# Marking it as generated collapses its diff on GitHub and keeps it out of
# language stats, so a review shows the few lines of data that actually changed
# instead of a 6,000-line regeneration. It is still committed and still tested.
index.html linguist-generated=true -diff
